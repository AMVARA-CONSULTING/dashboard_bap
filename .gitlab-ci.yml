cache:
  paths:
    - node_modules/
    
stages:
  - deploy

deploy:
  stage: deploy
  only:
    refs:
      - master
  script:
    - route=$(pwd)
    - npm install
    - npx ng build --prod --aot --build-optimizer --output-path=dist
    - sudo cp -rf ./dist/* /home/amvara/projects/dip-more/